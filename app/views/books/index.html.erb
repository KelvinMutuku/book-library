<h1>Books</h1>

<% if @books.any? %>
  <ul>
    <% @books.each do |book| %>
      <li>
        <%= link_to book.title, book_path(book) %>  
        (<%= book.available? ? "Available ✅" : "Borrowed ❌" %>)
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No books available.</p>
<% end %>
